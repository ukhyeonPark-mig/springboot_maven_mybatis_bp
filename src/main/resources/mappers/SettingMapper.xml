<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bp.mapper.SettingMapper">

    <select id="findFirst" resultType="Setting">
        SELECT id, footer, version, terms, privacy, created_at, updated_at
        FROM settings
        ORDER BY id
        LIMIT 1
    </select>

    <insert id="insert" parameterType="Setting" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO settings (footer, version, terms, privacy)
        VALUES (#{footer}, #{version}, #{terms}, #{privacy})
    </insert>

    <update id="update" parameterType="Setting">
        UPDATE settings
        SET footer  = #{footer},
            version = #{version},
            terms   = #{terms},
            privacy = #{privacy}
        WHERE id = #{id}
    </update>

</mapper>
